package dev.arzcbnh.tekoha.auth;

public class LoginDialog {
    //    public static final String PATH = "auth/login";
    //    public static final String PASSWORD_INPUT_KEY = "password";
    //
    //    private static MutableComponent createComponent(String id) {
    //        return Component.translatable(String.format("dialog.%s.auth.%s", TekohaAdditions.MOD_ID, id));
    //    }
    //
    //    public static Dialog createInstance( int passwordMaxLength) {
    //        final var infoMsg = new PlainMessage(createComponent("login"), PlainMessage.DEFAULT_WIDTH);
    //        final var errorMsg = new PlainMessage(createComponent("mismatch"), PlainMessage.DEFAULT_WIDTH);
    //        final var passwordInput = new Input(PASSWORD_INPUT_KEY, new TextInput(CommonButtonData.DEFAULT_WIDTH,
    // createComponent("password"), true, "", passwordMaxLength, Optional.empty()));
    //        final var common = new CommonDialogData(createComponent("title"), Optional.empty(), false, false,
    // DialogAction.NONE, List.of(infoMsg, errorMsg), List.of(passwordInput));
    //
    //        final var confirmAction = new CustomAll(ResourceLocation.fromNamespaceAndPath(TekohaAdditions.MOD_ID,
    // path), Optional.empty());
    //        final var confirmButton = new ActionButton(new CommonButtonData(createComponent("confirm"),
    // CommonButtonData.DEFAULT_WIDTH), Optional.of(confirmAction));
    //
    //        return new NoticeDialog(common, confirmButton);
    //    }
}
